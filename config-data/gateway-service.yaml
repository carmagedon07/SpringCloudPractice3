server:
  port: 8080
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    hostname: localhost

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: user-service
          uri: lb://user-servie
          predicates:
            - path=/user/**
        - id: car-service
          uri: lb://car-servie
          predicates:
            - path=/user/**
        - id: bike-service
          uri: lb://bike-servie
          predicates:
            - path=/bike/**
