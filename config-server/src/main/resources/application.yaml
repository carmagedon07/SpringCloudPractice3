server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          #default-label: config-service multiple-instances
          #default-label: eureka
          default-label: gateway
          uri: https://github.com/carmagedon07/SpringCloudPractice3
          search-paths: config-data

  application:
    name: config-service

